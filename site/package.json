{
  "name": "readease-landing",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4173 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173 --strictPort",
    "test": "node --import tsx --test tests/*.test.jsx",
    "verify": "npm test && npm run build && node --test tests/build.test.mjs"
  },
  "engines": { "node": ">=22.12.0" },
  "dependencies": { "react": "19.3.0", "react-dom": "19.3.0", "gsap": "3.15.0" },
  "devDependencies": { "vite": "8.3.0", "tsx": "4.23.15" }
}
